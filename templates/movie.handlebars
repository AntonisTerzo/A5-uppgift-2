<article class="movie">
  <div class="movie__container">
  <h1>{{ movie.title }}</h1>
  <div class="movie__intro">
    {{{ movie.intro }}}
  </div>
  </div>
  {{#if movie.image }}
  <img src="{{ movie.image.url }}">
  {{/if }}
</article>
